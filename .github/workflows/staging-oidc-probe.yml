name: Staging OIDC STS Probe

on:
  workflow_dispatch:
    inputs:
      role_arn:
        required: true
        type: string

permissions:
  contents: read

jobs:
  run:
    if: github.ref == 'refs/heads/codex/platform-release-automation'
    permissions:
      contents: read
      id-token: write
    uses: TrinyxAI/Trinyx/.github/workflows/staging-oidc-probe-impl.yml@1340ad64ac358694a3f88848db4665769da82f0c
    with:
      role_arn: ${{ inputs.role_arn }}
