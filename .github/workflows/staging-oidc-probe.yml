name: Staging OIDC STS Probe

on:
  workflow_dispatch:
    inputs:
      role_arn:
        required: true
        type: string

permissions:
  contents: read

jobs:
  run:
    if: github.ref == 'refs/heads/codex/platform-release-automation'
    permissions:
      contents: read
      id-token: write
    uses: TrinyxAI/Trinyx/.github/workflows/staging-oidc-probe-impl.yml@f5fc438d9d0ce785e5a6716e41be199bc97591e0
    with:
      role_arn: ${{ inputs.role_arn }}
