name: Staging OIDC STS Probe

on:
  workflow_dispatch:
    inputs:
      role_arn:
        required: true
        type: string

permissions:
  contents: read

jobs:
  run:
    if: github.ref == 'refs/heads/codex/platform-release-automation'
    permissions:
      contents: read
      id-token: write
    uses: TrinyxAI/Trinyx/.github/workflows/staging-oidc-probe-impl.yml@9954d6b56a5d06b9d053cd8748df50bcf516a82f
    with:
      role_arn: ${{ inputs.role_arn }}
