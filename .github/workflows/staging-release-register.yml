name: Register Staging Release

on:
  workflow_dispatch:
    inputs:
      run_id:
        required: true
        type: string
        default: '33485509832'
      artifact_id:
        required: true
        type: string
        default: '9791964215'
      artifact_digest:
        required: true
        type: string
        default: 'sha256:755594078d9da7e19406e01187534132920a31f87804c1b33baa28fa96559152'
      source_commit:
        required: true
        type: string
        default: 'f3a4c1ddcf6a17bfc837071f9046ac4c38a38b47'
      release_id:
        required: true
        type: string
        default: 'rel-v1-b5ba70c23b9f529ac8228a7b00b4faa4'
      bundle_digest:
        required: true
        type: string
        default: 'sha256:c9df14dcd1dbc24b31b926d3778bef2e208b59824c78f24292608284f3579892'
      registry_bucket:
        required: true
        type: string
      publisher_role_arn:
        required: true
        type: string

permissions:
  contents: read

jobs:
  run:
    if: github.ref == 'refs/heads/codex/platform-release-automation'
    permissions:
      actions: read
      attestations: write
      contents: read
      id-token: write
    uses: ./.github/workflows/staging-release-register-impl.yml
    with:
      run_id: ${{ inputs.run_id }}
      artifact_id: ${{ inputs.artifact_id }}
      artifact_digest: ${{ inputs.artifact_digest }}
      source_commit: ${{ inputs.source_commit }}
      release_id: ${{ inputs.release_id }}
      bundle_digest: ${{ inputs.bundle_digest }}
      registry_bucket: ${{ inputs.registry_bucket }}
      publisher_role_arn: ${{ inputs.publisher_role_arn }}
