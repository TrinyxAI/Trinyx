'use client';

/**
 * The skills list' create/rename folder dialog. The dialog itself is shared with the other
 * list pages that have folders - it lives in {@code components/folders/FolderNameDialog}.
 */
export { FolderNameDialog as CreateFolderDialog } from '@/components/folders/FolderNameDialog';
